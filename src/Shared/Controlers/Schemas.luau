local Schemas = {}

function Schemas:SchemaCheck(_SchemaPrefix, _SchemaList)
	-- will get the SchemaPrefix.."Schemas" schema and check if the given SchemaList follows the schema rules of it , returns true if yes
end

return Schemas
