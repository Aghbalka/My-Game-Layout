local Remotes = {}

Remotes.ToClient = {}
Remotes.ToServer = {}

function Remotes:CreateRemotes()
	-- will get every remote in every remotes module to create it, Event or Function
end

function Remotes:GetRemote(_RemoteName)
	-- will get the remote called "RemoteName" and return it
end

return Remotes
