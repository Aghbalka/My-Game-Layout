local Interface = {}

function Interface:ActivateInterface()
	-- will go throught every interface module , use the path to find the real object , and sync them to player's interaction
end

return Interface
